<!DOCTYPE html>
<html>

<head>
    <% include ../partials/header %>
</head>

<body>

    <div class="wrapper">
        <!-- Sidebar Holder -->
        <% include ../partials/latAdmin %>
        <!-- Page Content Holder -->
        <div id="content">

            <nav class="navbar navbar-default">
                <div class="container-fluid">

                    <div class="navbar-header">
                        <button type="button" id="sidebarCollapse" class="btn btn-info navbar-btn">
                            <i class="glyphicon glyphicon-leaf"></i>
                            <span></span>
                        </button>
                    </div>

                    <div style="float: right;">
                        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                            <form action="/logout">
                                <button type="submit" class="btn btn-info navbar-btn">Cerrar Sesión</button>
                            </form>
                        </div>
                    </div>
                </div>
                <div class="container-fluid">
                    <button type="submit" id="savebutton-editar" class="btn btn-info navbar-btn">Guardar cambios</button>
                    <div style="float: right;">
                        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                            <button type="submit" onclick="window.location.replace('/asociaciones');" class="btn btn-info navbar-btn">Asociaciones</button>
                        </div>
                    </div>
                </div>
                <div style="margin: 20px;">
                    
                    <h3>Administración/Asadas/Asociaciones/Editar asociación</h3>

                    <div class="form-group">
                        <label for="Nombre">Nombre</label>
                        <input required type="text" class="form-control" id="Nombre" placeholder="Nombre de la asociación"
                            name="Nombre" value="<%=asociacion.Nombre%>">
                    </div>

                    <div class="form-group">
                        <label for="asadaID">Agregar una ASADA</label>
                        <div class="input-group align-items-center justify-content-center">

                            <select id="asadaID" name="asadaID" class="form-control">
                                <%asadas.forEach(function(asada){%>
                                <option value="<%=asada.ID%>"><%=asada.Nombre%></option>
                                <%});%>
                            </select>
                            <span class="input-group-btn" style="margin-bottom: 3px;">
                                <button id="btn-add-asada" class="btn btn-info navbar-btn" type="button"> <i
                                        class="glyphicon glyphicon-plus"></i></button>
                            </span>
                        </div>
                    </div>

                    <div style="margin: 20px;">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th scope="col">ID</th>
                                    <th scope="col">Nombre</th>
                                    <th scope="col">Ver</th>
                                    <th scope="col">Eliminar</th>
                                </tr>
                            </thead>
                            <tbody id="asada_table_body">
                                    <%lista_asadas.forEach(function(asada){%>
                                        <tr id="asada_row_id_<%=asada.ID%>">
                                                <td id="asada_row_id_<%=asada.ID%>_ID"><%=asada.ID%></td>
                                                <td id="asada_row_id_<%=asada.ID%>_name"><%=asada.Nombre%></td>
                                                <td><i class="glyphicon glyphicon-leaf" style="color: #325276" onclick="location.href='/asadas/<%=asada.ID%>';"></i></td>
                                                <td><i class="glyphicon glyphicon-trash" onclick="deleteAsada('<%=asada.ID%>');"></i></td>
                                            </tr>
                                    <%});%>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div id="divbutton" style=" position: absolute; bottom: 6px; right: 8px;">
                </div>
            </nav>
        </div>
    </div>


    <!-- jQuery CDN -->
    <script src="https://code.jquery.com/jquery-1.12.0.min.js"></script>
    <!-- Bootstrap Js CDN -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <!-- jQuery Custom Scroller CDN -->
    <script
        src="https://cdnjs.cloudflare.com/ajax/libs/malihu-custom-scrollbar-plugin/3.1.5/jquery.mCustomScrollbar.concat.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {


            $('#sidebarCollapse').on('click', function () {
                $('#sidebar, #content').toggleClass('active');
                $('.collapse.in').toggleClass('in');
                $('a[aria-expanded=true]').attr('aria-expanded', 'false');
            });

            $("#divrisk").click(function () {
                $("#divrisk").toggle("slow", "linear");
            });

            $("#buttonrisk").click(function () {
                $("#divrisk").toggle("slow", "linear");
            });

        });
        let actuales = {
            
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
    <script src="/scripts/asociaciones.js"></script>
</body>

</html>